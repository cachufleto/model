{% for article in mesArticles %}
    <h3>{{ article.title }}</h3>
    <p>{{ article.body }}</p>
    <p><i>{{ article.published | date}}</i> -- {{article.user.signature}} </p>
    <br/>
{% else %}
    Pas d'article trouvé !
{% endfor %}